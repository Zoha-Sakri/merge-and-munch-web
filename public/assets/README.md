# Official logo asset

The official logo is installed as `public/assets/merge-and-munch-logo.png`.

The original supplied artwork is a landscape image; it was center-cropped into a 720 × 720 PNG so the illustrated mark remains legible in the compact game header. The responsive `GameLogo` component preserves the image ratio and uses the styled wordmark only if the asset fails to load.
