# Web Project Context

The Web client in `src/` is the feature reference for Merge & Munch, an original farming-city game. Core systems are crops, cattle goods, merge production, customer orders, coins/stars, community buildings, decorations, and Ancient City exploration.

Do not copy another game's protected identity or content. Prefer small, accessible React changes. Run `npm test && npm run build` after gameplay edits.