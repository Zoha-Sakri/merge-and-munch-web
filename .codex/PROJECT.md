# Web Codex Context

Implement original farming-city gameplay in React/TypeScript. The intended loop is farm production -> merge -> trade orders -> town upgrades -> tunnel exploration. Preserve the existing visual language and responsive layout.

Validation: `npm test && npm run build`. Never present Farm City assets, branding, text, or exact progression as Merge & Munch content.